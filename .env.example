# GitHub Configuration
# Get this from: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos only)
GITHUB_TOKEN=ghp_your_github_personal_access_token_here

# Webhook Secret
# You'll create this when setting up the webhook in your GitHub repo
# Use a strong random string (you can generate one with: openssl rand -hex 32)
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here

# Your API Endpoint
# This is where the PR data will be sent
API_ENDPOINT=https://your-api.com/analyze

# Server Port (optional, defaults to 3000)
PORT=3000
